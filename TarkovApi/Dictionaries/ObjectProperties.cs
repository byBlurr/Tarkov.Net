namespace Tarkov.Dictionaries
{
    public class ObjectProperties
    {
        // Starting off only getting id for objects, adding more as needed...

        public static readonly string Task = "id tarkovDataId name normalizedName trader { id } map { id } experience wikiLink minPlayerLevel taskRequirements { task { id } } traderRequirements { id } objectives { id } startRewards { items { item { id } } } finishRewards { items { item { id } } } failConditions { id } failureOutcome { items { item { id } } } restartable factionName neededKeys { keys { id } } kappaRequired lightkeeperRequired descriptionMessageId startMessageId successMessageId failMessageId";

    }
}
